# 已知问题与版本路线图

## 版本历史
| 版本 | 日期 | 修改人 | 修改内容 |
|------|------|--------|----------|
| v1.0 | 2026-06-08 | AI | 初始版本 |

## 1. 已知 Bug

### B0. 代码未推送导致 ECS 部署不生效（部署）🆕

| 项目 | 内容 |
|------|------|
| **严重程度** | **极高**（2026-06-11 已修复） |
| **位置** | 整个 Git 仓库 |
| **问题** | 1. 所有新功能代码从未 commit（`git status` 显示全部 `??`）<br>2. 从未配置 remote（`git remote -v` 为空）<br>3. `jl-shiyi-h5` 因嵌套 `.git` 被识别为 submodule，GitHub 只存储指针<br>4. 本地工作在 `feature/trade-status-display` 分支，但 CI 只监听 `main` |
| **影响** | ECS 运行的是几周前的旧版本，管理员面板、头像上传、资料编辑等新功能均未生效 |
| **建议** | 1. **首次提交前创建 `.gitignore`**<br>2. **add → commit → push 到 CI 监听的分支**<br>3. **不要嵌套 Git 仓库**（子目录有 `.git` 会导致外层将其视为 submodule）<br>4. **push 后验证**：`git log --oneline -5`、`git ls-files -s <关键目录>`（应显示 `100644` 而非 `160000`） |

### B1. 详情页 contact 字段直接暴露（安全）

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **位置** | `miniprogram/pages/bookDetail/bookDetail.js` → `loadBookDetail()` |
| **问题** | `loadBookDetail()` 使用 `db.collection('books').doc(id).get()` 直接读取数据库，未通过 `.field({ contact: false })` 排除 `contact` 字段。虽然前端不会主动展示该字段，但数据在 Network 面板可见 |
| **建议修复** | 在 `get()` 后加上 `.field({ contact: false })` |

### B2. Google Books API Key 硬编码（安全）

| 项目 | 内容 |
|------|------|
| **严重程度** | 高 |
| **位置** | `cloudfunctions/searchByISBN/index.js` |
| **问题** | Google Books API Key `AIzaSyB72FPpbJAJ9hO3x7FFp3ICf_184AFBDJA` 明文硬编码在代码中，任何可查看云函数代码的人都能获取 |
| **建议修复** | 将 API Key 迁移到云函数环境变量中 |

### B3. 评价快捷标签不可点击

| 项目 | 内容 |
|------|------|
| **严重程度** | 低 |
| **位置** | `miniprogram/pages/bookDetail/bookDetail.wxml` 评价弹窗区域 |
| **问题** | 评价弹窗中的快捷标签（"爽快交易"、"书况如实"、"沟通顺畅"）仅作为展示，没有绑定 `bindtap` 事件，用户无法选择 |
| **建议修复** | 添加点击事件，将选中的标签存入 `evaluationTags` |

### B4. 库存耗尽判断存在竞态窗口

| 项目 | 内容 |
|------|------|
| **严重程度** | 低（云函数单实例串行执行，实际风险极低） |
| **位置** | `cloudfunctions/expressInterest/index.js` |
| **问题** | 检查 `interestedUsers.length < quantity` 和写入 `interestedUsers` 之间存在时间窗口，理论上高并发下可能超过库存 |
| **建议修复** | 使用云数据库事务或乐观锁，但 v1 实际并发量下可暂不处理 |

### B5. 头像昵称恒为默认值

| 项目 | 内容 |
|------|------|
| **严重程度** | 中 |
| **位置** | `miniprogram/app.js` → `syncUserInfo()` + `miniprogram/pages/myBooks/myBooks.js` |
| **问题** | 微信已废弃 `wx.getUserInfo` 接口（2021年4月起只能返回默认灰色头像和"微信用户"），当前 `syncUserInfo()` 中 `wx.getUserInfo()` 调用只能获取到默认值。所有用户头像显示为灰色方块，昵称恒为"拾遗用户" |
| **建议修复** | 规划中，见 3.2 |

### B6. 书城列表不完整

| 项目 | 内容 |
|------|------|
| **严重程度** | 低 |
| **位置** | `cloudfunctions/getBookList/index.js` |
| **问题** | 当前仅返回 `status='available'` 且 `isLocked=false` 的书籍，被锁定但未售出的书在书城中不可见 |
| **建议** | 评估是否需要新增"已锁定"筛选项（可选） |

---

## 2. 代码质量问题

### C1. 详情页绕过云函数直接读库

`bookDetail.js` 的 `loadBookDetail()` 直接通过 `db.collection('books').doc(id).get()` 读取完整书籍对象，而其他查询都走云函数。这导致：
- `contact` 字段可能暴露
- 业务逻辑分散在页面和云函数两处

**建议**：统一改为调用云函数（如新增 `getBookDetail` 云函数），或者至少在前端查询时 `field` 排除 `contact`。

### C2. 空目录残留

| 目录 | 状态 |
|------|------|
| `miniprogram/pages/example/` | 空，未使用 |
| `miniprogram/pages/index/` | 空，未使用 |
| `miniprogram/components/cloudTipModal/` | 空，未使用 |

**建议**：清理或实现。

### C3. 未使用自定义组件

编码规范中规划了 4 个自定义组件（`book-card`、`search-bar`、`rating`、`condition-tag`），但实际代码中未实现，所有 UI 直接在页面内编写。

**建议**：在 v2 重构时抽取为自定义组件，提高复用性。

---

## 3. 版本路线图

### 3.1 v1.0（当前版本 - MVP）

**已实现**：

| 模块 | 功能 | 状态 |
|------|------|------|
| 登录 | 云开发自动鉴权 | ✅ |
| 书城 | 列表展示、分页 | ✅ |
| 书城 | 分类筛选 | ✅ |
| 书城 | 校区/学院/专业三级筛选 | ✅ |
| 书城 | 排序（时间/价格） | ✅ |
| 书城 | 关键词搜索（防抖） | ✅ |
| 发布新书 | ISBN 扫码/手动录入 | ✅ |
| 发布新书 | 图片压缩上传（1-5张） | ✅ |
| 发布新书 | 自动关联校区 | ✅ |
| 详情页 | 完整信息展示 | ✅ |
| 详情页 | "我想买"→获取联系方式 | ✅ |
| 详情页 | 卖家确认售出 | ✅ |
| 详情页 | 双向评价系统 | ✅ |
| 详情页 | 交易留言板 | ✅ |
| 详情页 | 举报机制 | ✅ |
| 自己的书 | 在售/已售罄两Tab | ✅ |
| 自己的书 | 下架书籍 | ✅ |
| 自己的书 | 数据修复 | ✅ |
| 分享 | 分享到聊天/朋友圈 | ✅ |

**待修复的 Bug**：B1, B2, B3

---

### 3.2 v1.1（计划 - 体验优化）

| 优先级 | 功能 | 说明 |
|-------|------|------|
| P0 | **用户头像昵称修改** | 在"自己的书"页面顶部添加：`<button open-type="chooseAvatar">` 修改头像 + `<input type="nickname">` 修改昵称，头像上传到云存储 |
| P0 | 修复 B1 | `loadBookDetail()` 添加 `.field({ contact: false })` |
| P0 | 修复 B2 | API Key 迁移到环境变量 |
| P1 | 修复 B3 | 评价快捷标签添点击事件 |
| P1 | 清理空目录 | 删除 example/index/cloudTipModal |
| P2 | 联系方式复制后提示 | 复制联系方式后 Toast 反馈 |

---

### 3.3 v2.0（计划 - 增强功能）

| 优先级 | 功能 | 说明 |
|-------|------|------|
| P0 | **微信订阅消息通知** | "有人想买"时通知卖家、"已售出"时通知买家 |
| P1 | **编辑书籍** | 发布后修改价格、描述、图片等字段（`editBook/` 页面） |
| P1 | **内容安全审核** | 接入 `security.imgSecCheck` + `security.msgSecCheck`，发布时自动审核 |
| P1 | **联系人脱敏展示** | 联系电话/微信号脱敏（如"138****1234"） |
| P2 | **用户信用页** | 查看用户历史交易评价、信用分（`userProfile/` 页面） |
| P2 | **自定义组件抽取** | 将书籍卡片、搜索栏、星级评价、标签等抽取为独立组件 |
| P2 | **管理员后台** | 查看和处理举报（`reports` 集合管理） |
| P3 | **按距离排序** | 同一校区的书优先展示 |
| P3 | **收藏/书单** | 用户可收藏想买但暂不联系的书 |

---

## 4. 技术债务

| 编号 | 问题 | 优先级 | 建议 |
|------|------|-------|------|
| TD1 | `db.RegExp` 模糊搜索性能差 | 中 | 数据量 < 5000 时可用；超过后考虑 Elasticsearch 或外部搜索服务 |
| TD2 | 云函数无统一错误处理中间件 | 低 | 当前每个函数内 try-catch，可抽取公共错误处理 |
| TD3 | 无自动化测试 | 低 | v2 建议添加核心流程的集成测试 |
| TD4 | 无数据备份方案 | 中 | 建议定期导出云数据库数据备份 |

---

## 5. 安全性待办

| 编号 | 事项 | 状态 |
|------|------|------|
| S1 | Google Books API Key 硬编码 → 环境变量 | ☐ |
| S2 | `contact` 字段前端查询排除 | ☐ |
| S3 | 内容安全审核（图片/文字） | ☐ |
| S4 | 用户输入 XSS 防护 | ✅ 小程序原生沙箱已防护 |
| S5 | 云数据库权限最小化 | ✅ 已配置各集合权限 |
| S6 | 图片路径不包含 openid | ✅ 使用 `books/{timestamp}_{random}` |
| S7 | 发布前代码压缩 | ✅ project.config.json 已开启 |
